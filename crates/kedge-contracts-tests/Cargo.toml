[package]
name = "kedge-contracts-tests"
edition = "2024"
version.workspace = true
license.workspace = true

[dependencies]
kedge-contracts = { path = "../kedge-contracts", features = [
  "flux-rs",
  "kani",
] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
